# DataGuard 首轮开发基线

## 基本信息

- 日期：2026-08-09（Asia/Shanghai，UTC+08:00）
- 目标目录：`E:\cybersecurity\DataGuard`
- 当前提交：不适用；目标目录不是 Git 工作树，无法取得提交 SHA
- 当前分支：不适用；目标目录不是 Git 工作树
- 变更边界：未修改业务代码；本轮仅新增本开发基线文档及其独立目录

## 当前仓库状态

检查开始时，`E:\cybersecurity\DataGuard` 是一个普通空目录，不包含 `.git`、源代码、依赖清单、构建配置、README 或项目级 `AGENTS.md`。本轮检查期间，测试侧新增了独立的 `docs/testing` 基线文档；该文档不构成 DataGuard 业务仓库、产品源码或构建入口。

因此当前路径仍不能被视为可开发的 DataGuard Git 工作树，也无法建立以提交或分支为锚点的开发基线。`E:\cybersecurity` 本身同样不是 Git 工作树；同级 `AegisEval` 不属于本轮 DataGuard 开发范围。

## 代码结构与技术栈

- 业务代码结构：不存在或尚未同步，无法识别模块、分层、服务边界或程序入口。
- 技术栈：无法确认；未发现语言清单、包管理器锁文件、项目文件、容器配置或基础设施定义。
- 依赖关系：无法确认；未发现可解析的依赖声明或版本约束。
- 数据与接口：无法确认；未发现数据库迁移、模式定义、API 契约或消息协议。
- 开发约束：未发现适用的项目说明、编码规范、贡献指南或仓库级 `AGENTS.md`。

## 构建与开发入口

当前没有可执行或可确认的构建、安装、启动、静态检查及代码生成命令。不得根据目录名称猜测技术栈并执行 `npm`、`pip`、`dotnet`、`maven`、`gradle`、`cargo`、`go` 或其他依赖安装/构建命令。

待仓库内容就绪后，应优先从项目说明、锁文件、CI 配置和构建脚本确认官方入口，再记录可复现命令。当前构建命令记为：**无（阻塞）**。

## 开发风险与阻塞

### DG-DB-001：DataGuard 仓库内容缺失

- 类型：开发阻塞
- 严重程度：Blocker
- 现状：目标目录不存在 Git 元数据和业务项目文件。
- 影响：无法确认目标版本、需求对应代码、技术栈、模块边界、构建流程、依赖安全状态及变更影响面；不能开始可靠的业务实现。
- 解除条件：将正确的 DataGuard 仓库同步到目标目录，或提供正确仓库路径，并明确本轮目标分支/提交。

### 其他未决风险

- 缺少需求、验收标准和预期变更范围，无法拆分开发任务或评估兼容性。
- 缺少官方环境要求与依赖锁定信息，无法保证开发环境和构建的可复现性。
- 缺少数据分类、权限边界及安全约束，无法判断 DataGuard 涉及的敏感数据处理要求。
- 缺少 CI、代码质量门槛和发布流程信息；开发侧不得据此作出测试验收或发布批准结论。

## 待确认事项

1. DataGuard 的正确 Git 远程仓库或本地工作树位置是什么？
2. 下一轮应基于哪个分支和提交开展开发？
3. 本轮需求、范围边界、验收标准和优先级是什么？
4. 哪些 `AGENTS.md`、README、架构说明和安全规范适用于该仓库？
5. 官方依赖安装、构建、启动、代码生成、lint 和类型检查命令分别是什么？
6. 是否存在必须使用的开发配置、模拟服务或脱敏数据集？严禁默认使用生产凭据或生产数据。
7. 是否有受保护目录、生成文件、兼容性目标或禁止修改的公共接口？

## 下一轮开发上下文

仓库就绪后，开发侧应按以下顺序恢复工作：

1. 重新读取目标路径各层级适用的 `AGENTS.md` 和项目说明。
2. 记录 Git 根目录、远程信息、当前分支、提交 SHA、工作树状态及既有未提交变更；保留用户或其他 Agent 的改动。
3. 盘点源码、配置、依赖、构建脚本、CI、数据迁移和接口契约，形成实际代码结构与技术栈说明。
4. 依据项目声明的官方命令验证依赖解析和构建入口；不擅自升级依赖或更换工具链。
5. 在需求与验收标准明确后，给出开发侧影响分析和实施方案，再进行业务代码修改。
6. 将代码测试与验收交由测试职责方完成；开发侧只提供实现说明、已执行的开发检查及已知风险，不作最终架构或发布批准。

本文件只描述 2026-08-09 可核实的空仓库基线。后续仓库内容一旦同步，应以新的 Git 状态和项目说明为准更新开发上下文，不能把本文件中的“无技术栈/无构建命令”继续解释为当前项目事实。
