# Phase 5 development record — configurable Compose API port

## Scope

- Baseline: `main@247f42e1dea8d4feffd8afe0e2ad9ff894c104ca` with a clean working tree.
- Change only the Compose host-side API port so local IIS or another listener can coexist with DataGuard.
- Preserve the container API port at `8000`, the host bind at loopback only, and the default host port at `8000`.
- No product behavior, public API, model, fixture, policy, database schema, or dependency changed.
- No real `.env` file was read, and no container, model call, or evaluation run was started.

## Changes

| File | Change |
|---|---|
| `compose.yaml` | Bind `127.0.0.1:${DATAGUARD_API_PORT:-8000}:8000`. |
| `.env.example` | Declare the non-secret default `DATAGUARD_API_PORT=8000`. |
| `tests/unit/test_delivery_files.py` | Lock the loopback/default/override Compose expression and example default. |

Operators can leave the variable unset for the existing `127.0.0.1:8000`
behavior, or set `DATAGUARD_API_PORT` in their local Compose environment to an
available host port. The variable does not change the container healthcheck,
which correctly continues to use container-local port `8000`.

## Development-side evidence

| Command | Exit | Result |
|---|---:|---|
| `.\.venv\Scripts\python -m pytest tests\unit\test_delivery_files.py -q` | `0` | `6 passed in 0.07s`. |
| `docker compose version` | unavailable | PowerShell could not find `docker` in this environment. |
| `docker compose --env-file .env.example config --quiet` | not run | Blocked by the missing Docker/Compose executable; no real `.env` was read. |

This is development evidence only, not independent test or architecture
acceptance. Independent testing should run Compose config expansion once Docker
Compose is available and verify both the unset default and a non-8000 override.
