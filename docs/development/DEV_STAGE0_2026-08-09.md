# DataGuard Stage 0 Contract Development Record

## Record metadata

- Date: 2026-08-09 (Asia/Shanghai, UTC+08:00)
- Role: Stage 0 development Agent
- Target: `E:\cybersecurity\DataGuard`
- Git branch/commit: not applicable; the target and its parent are not Git work trees
- Scope: repository skeleton, charter, RAG/security architecture, public and
  machine-readable contracts, and engineering evidence templates only
- Excluded: business/source code, dependency installation, build, test
  execution, independent test acceptance, architecture acceptance, commit, push

No applicable `AGENTS.md` exists at `E:\`, `E:\cybersecurity`, or the DataGuard
root. The three dated baseline documents that existed at the start of this work
were read and preserved. A testing-side `TEST_STAGE0_PLAN_2026-08-09.md` appeared
during parallel work and was also left untouched.

## Delivered files

### Repository and project

- `LICENSE`: MIT, copyright DataGuard contributors.
- `.gitignore`: environment/secrets, editor, language-cache/build, and local
  runtime/evidence-output exclusions.
- `.gitattributes`: `text=auto` binary-safe detection plus repository text rules
  that normalize Markdown, YAML, JSON, LICENSE, `.gitignore`, and
  `.gitattributes` to LF, preventing cross-platform manifest/artifact digest drift.
- `README.md`: Stage 0 status, locked stack/models, public endpoint list, fixed
  baseline/guarded summary, document map, and no-runtime notice.
- `docs/PROJECT_CHARTER.md`: purpose, goals/non-goals, roles, fixed invariants,
  ownership, stage gates, metrics, and change control.

### Architecture, security, and governance

- `docs/architecture/SYSTEM_CONTEXT_AND_DATA_FLOW.md`: corpus/index build,
  versioned vector-index use in both modes, eight-step guarded flow, baseline
  sequence, trust boundaries, run state, storage topology, and evidence gates.
- `docs/security/THREAT_MODEL.md`: four fixed AttackFamily threats, malicious
  corpus documents, retrieval/context/model/evidence boundaries, Canary and
  protected-fragment risks, and evidence checks.
- `docs/security/RISK_TAXONOMY.md`: AttackFamily, DetectionType, outcomes,
  `indeterminate`, final-leak ASR, attack delivery, retrieval violation, false
  rejection, blocked-baseline attribution, and RAG-specific risk categories.
- `docs/security/DATA_GOVERNANCE_AND_SECURITY_BOUNDARIES.md`: purely synthetic
  governance, cumulative role/classification rules, marker handling, no-raw-text
  persistence, SQLite/PostgreSQL profiles, and local/Git artifact lifecycle.

### Machine-readable and compatibility contracts

- `docs/contracts/openapi.yaml`: the six approved endpoints and exact v1 public
  request/response shapes, audit evidence structures, run/report semantics,
  bounded health readiness, and RFC Problem Details extension schema.
  Audit failures use only an optional/nullable shared `error_code`; free-form
  audit reason text is excluded by the closed schema.
- `docs/contracts/error-codes.yaml`: 16 stable snake-case error codes plus the
  normal HTTP 200 guarded-block contract.
- `docs/contracts/metrics.yaml`: metric names/dimensions, no-high-cardinality or
  raw-content rules, final-return-only ASR, delivery/retrieval rates, authorized
  QA, false rejection, and blocked-baseline counting.
- `docs/contracts/report.schema.json`: Draft 2020-12 report schema with
  `portfolio_eligible`, `comparability_key`, locked runtime/digests/settings,
  sanitized ranked retrieval/denial/detection details, paired prevention stage,
  directly enforced 30 authorized-QA plus four 8-case bilingual attack-family
  distribution, conditional evidence/PostgreSQL/pass/no-indeterminate portfolio
  eligibility, summaries, and locked gates.
- `docs/contracts/experiment-manifest.schema.json`: strict PostgreSQL evidence
  manifest for actual Ollama version, exact tags/digests, observed embedding
  dimensions, stream false, distribution, prompt/policy/detector/index hashes,
  system Canary ID/hash, and detector normalization contract.
- `docs/contracts/identity-table.schema.json`: YAML-ready six-row synthetic
  identity table with exactly two subjects per role.
- `docs/contracts/corpus.schema.json`: YAML-ready 30-document contract with exact
  classification/language distribution, cumulative roles, short atomic content,
  exactly one document Canary, and role-bound protected fragments.
- `docs/contracts/scenario-set.schema.json`: YAML-ready 62-scenario contract with
  30 authorized-QA cases and 8 bilingual cases per AttackFamily.
- `docs/contracts/README.md`: normative index, canonical constants, eight-step
  guarded chain, gate semantics, report-state behavior, and compatibility rules.

### Work-record templates

- `docs/development/DEV_WORK_TEMPLATE.md`
- `docs/testing/TEST_WORK_TEMPLATE.md`
- `docs/architecture/ARCH_ACCEPTANCE_TEMPLATE.md`

## Locked architecture captured

- Stack: Python 3.12, FastAPI, Pydantic, SQLAlchemy, PyYAML, pytest.
- Local Ollama: generation `qwen2.5:3b-instruct`; embedding
  `qwen3-embedding:0.6b`; actual tags/digests/Ollama version/embedding dimensions
  are evidence-manifest values.
- Settings: temperature 0, seed 42, generation top-k 20, top-p 0.9, context
  8192, predict 512, retrieval top-k 4, stream false.
- Dataset: 6 identities (2/role), 30 short documents (10/classification and 5
  English + 5 Chinese within each), 62 scenarios (30 authorized QA, one per
  document; 32 attacks, 8/family and 4/language).
- Baseline: same vector index, all-corpus retrieval, unauthorized context allowed,
  weak isolation, shared detector observe-only.
- Guarded: subject resolution; role filter; top-4 retrieval; JSON document
  boundary; message isolation; full-output deterministic normalization/detection;
  whole-output discard plus exact fixed block on violation; minimized audit.
- Evidence gates: baseline ≥1 final disclosure per family and total ASR ≥20%;
  guarded leaks 0 and unauthorized context documents 0; authorized-QA factual
  pass ≥80%; false rejection ≤10%.
- Portfolio eligibility additionally requires zero indeterminate mode results;
  scheduled denominators remain fixed at 32 attacks and 8 cross-role attacks.
- Deterministic simulators are unit-test-only; chat/integration/regression/
  exploratory/evidence paths fail explicitly when local Ollama/models are
  unavailable, without silent substitution.

## Development-side verification performed

These checks validate document syntax and internal contract consistency only.
They are not product tests or acceptance.

| Check/command | Result |
| --- | --- |
| Read-only `Get-ChildItem`, `rg --files`, ancestor `AGENTS.md` checks, and `git status --short --branch` | Confirmed initial docs, no applicable instructions, and no Git work tree |
| Python `json.load` + `jsonschema.Draft202012Validator.check_schema` over `docs/contracts/*.schema.json` | PASS: corpus, experiment manifest, identity table, report, and scenario-set schemas valid Draft 2020-12 schemas |
| Python `yaml.safe_load` over `docs/contracts/*.yaml` | PASS: OpenAPI, error catalog, and metric catalog parse |
| Custom read-only cross-contract assertions | PASS: six paths; exact chat/evaluation shapes; run states; roles/modes/AttackFamily/DetectionType; 16-code equality; models/counts/stream; exact fixed reply; Canary-only hit details; singular prevention stage |
| Report-schema distribution and portfolio conditional assertions | PASS: 30 authorized-QA; 8 per family with 4 English/4 Chinese; portfolio true requires evidence/PostgreSQL/all required pass gates/no indeterminate |
| Audit minimization contract assertion | PASS: no free-form `reason_codes`; optional nullable `error_code` references the shared ErrorCode enum |
| Byte-level encoding/newline scan over Markdown, YAML/YML, JSON, LICENSE, `.gitignore`, and `.gitattributes` | PASS: strict UTF-8 without BOM; LF-only, with no CRLF or lone CR bytes |
| Markdown relative-link existence scan | PASS: no broken local links |
| Obsolete-contract scan for retired scenario/state values, old headers/codes/manifest name, and old request size, excluding historical/parallel evidence | PASS: no obsolete current-contract values |
| Credential-pattern scan for common cloud/GitHub/OpenAI token, private-key, password, and API-key forms | PASS: no credential-like values found |

## Existing material preserved

The following were not overwritten or edited:

- `docs/development/DEV_BASELINE_2026-08-09.md`
- `docs/testing/TEST_BASELINE_2026-08-09.md`
- `docs/architecture/ARCH_ACCEPTANCE_2026-08-09.md`
- `docs/testing/TEST_STAGE0_PLAN_2026-08-09.md` (parallel testing-side work)

## Known limitations and next context

1. This directory is not a Git work tree, so no branch, SHA, status diff, remote,
   commit, or push evidence can be recorded. No Git initialization was performed.
2. Stage 0 contains no business code, dependency manifest, database migration,
   Compose file, synthetic instance YAML, vector index, or generated report.
3. JSON Schema validates structure and many fixed distributions; implementation
   must also enforce cross-record uniqueness/reference rules, protected-fragment
   role equality, and exactly one authorized-QA document target per corpus document.
4. Actual local Ollama version, model digests, and embedding dimensions (expected
   model default 1024, actual value authoritative) must be captured later; no
   Ollama or database readiness check was run in this documentation-only work.
5. Audit/report interfaces model a local synthetic experiment and intentionally
   make no production authentication or reviewer-authorization claim.
6. Independent testing and architecture acceptance remain for their respective
   owners. This development record makes no pass/release determination.

No commit or push was run.
