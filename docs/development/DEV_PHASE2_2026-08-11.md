# Development work record: roadmap Phase 2

## Context and scope

- Date: 2026-08-11
- Baseline: `main@230aac98f1dc7a825825ea4977cd07acd28600ad`
- Scope: original seven-stage roadmap Phase 2, local Ollama/vector retrieval/
  Baseline RAG only.
- Restrictions: no Ollama installation/start/pull, no real network model call,
  no Phase 3-6 feature work, no commit or push.

The approved attachment was read as UTF-8 and incorporated verbatim, with only
repository LF normalization, into
`docs/architecture/SEVEN_STAGE_ROADMAP.md`. That document now defines the
authoritative project progress numbering. Earlier Stage 2 documents remain
implementation contracts but are broader than roadmap Phase 2 because prior
work combined multiple roadmap phases.

## Phase 2 gap audit

| Original Phase 2 requirement | Current implementation and test evidence | Gap decision |
| --- | --- | --- |
| Ollama version/tags/embed/chat | Bounded local-only adapter implements all four paths; probe also uses show only for embedding dimensions. `test_ollama_client.py` covers exact requests, models, errors, timeouts, bounds, and malformed protocol. | Implemented |
| Fixed generation/embedding models | Closed facts and request builders require `qwen2.5:3b-instruct` and `qwen3-embedding:0.6b`. | Implemented |
| 30-document index | `build-index` embeds the accepted corpus in one bounded request; canonical artifact is loaded into a validated in-memory handle. The persisted artifact adds binding and safe restart support without changing retrieval semantics. | Implemented |
| Cosine top-4 and doc-ID tie break | Vector-index core prefilters eligible IDs, computes bounded cosine scores, sorts descending then `doc_id`, and returns exactly four when available. | Implemented |
| Weak Baseline template and hash | Versioned Baseline resource is neutral/weak, hashed by the resource loader, and rendered as one user payload. | Implemented |
| Baseline `POST /v1/chat` | Production service performs one exact embedding, all-corpus Baseline plan, one chat generation, observe-only detection, and minimized audit. | Implemented |
| Health model/Ollama/digest facts | Production startup caches probed version/tags/digests/dimension and `/health` exposes the closed status without request-time probing. | Implemented |
| Explicit offline/model/timeout/protocol errors | Adapter and API map fixed codes/statuses, never use a production simulator, and tests cover dependency-down HTTP behavior. | Implemented |
| Unauthorized documents eligible/in context | Baseline passes all 30 IDs before scoring; planner tests demonstrate a guest can retrieve unauthorized documents and include selected bodies in the weak payload. | Implemented |
| Stable real retrieval order | Deterministic unit behavior exists, but the roadmap requires a real local-model repeatability observation. | Real run pending |
| Real embed and chat integration | Explicit CLI/server entrypoints exist; current host has no available Ollama evidence. | Real run pending |
| Controlled Baseline Canary/protected-fragment output | Shared observe-only detector and minimized audit exist; an actual fixed-model output must demonstrate at least one hit. | Real run pending |
| No automatic model pull | No production or delivery command invokes `ollama pull`; the runbook stops if tags are absent. | Implemented |
| Report-level environment/settings facts | Current later-phase manifest/report implementation binds actual Ollama facts, index, resource and locked settings. Phase 2 runbook captures the same minimized facts without claiming a Phase 4 report. | Implemented; Phase 2 capture pending |

No missing product entrypoint or validation blocked a real Phase 2 run. Adding a
second Phase-2-only server, simulator, or index implementation would duplicate
the accepted production surfaces, so this batch intentionally adds no product
code. The only material gap was an isolated real-Ollama acceptance procedure.

## Files changed

- `docs/architecture/SEVEN_STAGE_ROADMAP.md`
- `docs/testing/PHASE2_REAL_OLLAMA_ACCEPTANCE.md`
- `docs/development/DEV_PHASE2_2026-08-11.md`

Existing `.gitattributes` and `.dockerignore` LF corrections were already
present in the worktree at task start and were not changed by this batch.

## Development verification

| Command/check | Exit code | Result |
| --- | ---: | --- |
| Phase 2 targeted pytest selection | 0 | 103 passed in 8.34s |
| Roadmap attachment comparison after CRLF-to-LF normalization | 0 | Exact equality, 5,942 characters |
| `git diff --check` | 0 | No whitespace errors |

The targeted selection covers the complete Ollama adapter test file plus the
exact 30-document embedding request, deterministic top-4/tie retrieval,
all-corpus unauthorized Baseline candidate, observe-only Baseline execution,
production chat/audit, cached Ollama dependency failure, and minimized artifact
CLI dependency failure.

This is a documentation-only/no-product-code batch. The three new Markdown
files are the only worktree changes. This record is developer evidence only; it
is not independent testing or Phase 2 architecture acceptance. Real Ollama
checks are NOT RUN on this host and no result is simulated. No commit or push
was performed.
