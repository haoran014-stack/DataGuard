# DataGuard Development Work Record — Stage 2 Batch A1

## Record metadata

- Date/time and timezone: 2026-08-09 20:11:53 +08:00
- Development owner: DataGuard development Agent
- Requirement/work item IDs: S2-ENV, Stage 2 Batch A1
- Repository root: `E:/cybersecurity/DataGuard`
- Baseline branch and commit SHA: clean `main` at
  `78923203816e7ef4478157999f43c1e09153e592`, equal to `origin/main`
  before work.
- Working-tree status before work: `## main...origin/main`, with no tracked,
  untracked, or staged changes.
- Scope: exact Stage 2 direct runtime dependencies, closed side-effect-free
  runtime settings, local-only Ollama URL validation, bounded timeout/response/
  runtime-path settings, minimized database DSN handling, and configuration
  unit tests.
- Explicit non-goals: prompts/resources, Ollama adapter or network call,
  detector, index, RAG, storage implementation, FastAPI application/endpoints,
  evaluation, reports, metrics, Compose, dependency lock generation, commit,
  and push.

This is a development-side Batch A1 record. It is not independent test or
architecture acceptance, release evidence, or proof that the Stage 2 runtime is
implemented.

## Inputs and applicable instructions

- Applicable `AGENTS.md` files: none found in the repository or checked
  workspace ancestors.
- Architecture/security authority reviewed completely:
  [Stage 2 scope and acceptance](../architecture/STAGE2_SCOPE_AND_ACCEPTANCE.md).
- All machine contracts under `docs/contracts/` were reviewed without change:
  `openapi.yaml`, `error-codes.yaml`, `metrics.yaml`, `report.schema.json`,
  `report-semantic-rules.yaml`, `experiment-manifest.schema.json`,
  `identity-table.schema.json`, `corpus.schema.json`, and
  `scenario-set.schema.json`; the [contract index](../contracts/README.md) was
  also reviewed.
- Existing implementation reviewed and preserved: Stage 1 domain models,
  three `synthetic-v1` fixtures, layered fixture/error/report/API validators,
  validation CLI, and all 147 accepted developer-side unit tests.
- Existing user/Agent changes preserved: none; the assigned baseline was clean.

## Impact analysis

| Area | Impact | Compatibility decision |
| --- | --- | --- |
| HTTP API | No application or endpoint exists in A1; `uvicorn` is pinned only as the later ASGI runner | none |
| Error codes | No code, catalog, or Problem Details change | none |
| Metrics | No metric implementation or contract change | none |
| Report/manifest/data schemas | No producer or schema change | none |
| RAG flow/trust boundaries | Configuration rejects nonlocal Ollama endpoints and keeps database DSNs secret; no RAG flow exists | none |
| Database/storage | PostgreSQL driver is available for a later repository; A1 creates no engine, connection, file, table, or migration | none |

## Dependency decisions

| Direct dependency | Exact pin | A1 decision |
| --- | --- | --- |
| `httpx` | `0.28.1` | Required by the later bounded Ollama HTTP adapter; A1 performs no model request |
| `uvicorn` | `0.51.0` | Required to run the later ASGI application factory; A1 intentionally adds no false/nonfunctional DataGuard server entry point |
| `psycopg[binary]` | `3.3.4` | Required by the later PostgreSQL evidence repository; PyPI index showed matching `psycopg` and `psycopg-binary` stable versions and the CPython 3.12 Windows wheel installed successfully |

The existing Python `>=3.12,<3.13` boundary and all existing exact direct pins
remain unchanged. A reviewed hashed transitive lock is deliberately deferred to
a later Stage 2 batch, as authorized for A1; this remains a release/evidence
gate.

## A1 corrections

| Correction | Development result |
| --- | --- |
| PostgreSQL port validation | `RuntimeSettings` now safely reads `urlsplit(...).port`; a missing port remains valid, while a supplied port must be numeric and within 1..65535. Non-numeric, zero, and overflow ports use the same minimized backend-mismatch error without echoing the DSN or credential sentinel. |

## Implementation summary

| File | Change | Reason |
| --- | --- | --- |
| `pyproject.toml` | Added only the three exact direct dependencies above | Later ASGI, local Ollama HTTP, and PostgreSQL runtime prerequisites |
| `src/dataguard/config.py` | Added frozen `extra=forbid` settings, explicit environment loading, local-only Ollama URL rules, numeric/path bounds, storage/profile coupling, and excluded secret DSN serialization | S2-ENV closed/local/minimized configuration foundation |
| `tests/unit/test_config.py` | Added 40 positive, negative, secret-minimization, and import-side-effect tests | A1 developer verification without live model/database/API use |
| `docs/development/DEV_STAGE2_2026-08-09.md` | Added this bounded development record | Reproducible handoff without rewriting Stage 0/1 history |

No file under `docs/contracts/` or `docs/architecture/` was modified.

## Fixed architecture conformance

- [x] `RuntimeSettings` is frozen and closed (`extra=forbid`), validates its
      defaults, and hides validation inputs.
- [x] Importing `dataguard.config` does not read environment variables or make
      network, database, or filesystem calls.
- [x] Default profile/storage are exploratory/SQLite; the default runtime state
      path remains relative beneath ignored `artifacts/`.
- [x] Evidence profile requires PostgreSQL and a `postgresql+psycopg` DSN.
- [x] PostgreSQL DSNs may omit the port; an explicit port is safely parsed and
      must be numeric and within 1..65535, with content-free validation failure.
- [x] Ollama defaults to `http://127.0.0.1:11434` and accepts only HTTP with the
      exact hosts `localhost`, `127.0.0.1`, or `[::1]`, no userinfo, query,
      fragment, or non-root URL path.
- [x] Connect/read timeout, response-byte, URL/DSN length, and runtime path
      boundaries are explicit and tested.
- [x] Database DSN is excluded from model repr/serialization and validation
      input detail; only an explicit internal accessor can retrieve it.
- [x] No DSN credential value is present in this record or a public response.
- [ ] Prompt resources and Ollama adapter: intentionally deferred beyond A1.
- [ ] Detector/index/RAG/storage/API/evaluation/report/metrics/Compose:
      intentionally outside A1.
- [ ] Hashed transitive lock and clean `--require-hashes` install: deferred Stage
      2 release/evidence requirement.

## Development-side checks

These commands are development evidence only, not independent acceptance.

| Command | Exit code | Result/evidence |
| --- | ---: | --- |
| Initial parallel `pip index versions psycopg` / `psycopg-binary` query | 124 | Group reached the 30-second command limit; the binary index list was returned but this was not treated as successful evidence |
| `.venv\Scripts\python -m pip index versions psycopg --timeout 15 --retries 0` | 0 | Stable version reported as `3.3.4` |
| First individual `psycopg-binary` index query | 124 | Index list reported `3.3.4`, then pip's own latest-version check kept the command beyond 30 seconds |
| `.venv\Scripts\python -m pip --disable-pip-version-check index versions psycopg-binary --timeout 15 --retries 0` | 0 | Stable version reported as `3.3.4` |
| First `.venv\Scripts\python -m pytest tests/unit/test_config.py` | 1 | 32 passed; one setup error was WinError 5 creating pytest's system-temp root, before the import-side-effect test body ran |
| Same configuration test after repository-ignored temp fixture | 0 | 33 passed in 0.50s |
| First `.venv\Scripts\python -m pip install -e ".[dev]"` | 124 | Dependency resolution succeeded, but the slow binary-wheel download exceeded the 120-second command limit; pins were not changed |
| `.venv\Scripts\python -m pip --disable-pip-version-check install -e ".[dev]" --timeout 60 --retries 1` | 0 | Editable install completed with the exact direct dependencies and CPython 3.12 Windows binary driver |
| Installed dependency inventory | 0 | `httpx=0.28.1`, `uvicorn=0.51.0`, `psycopg=3.3.4`, `psycopg-binary=3.3.4` |
| `.venv\Scripts\python -m pytest` before final empty-query/fragment/port hardening | 0 | 180 passed in 11.24s: original 147 plus the then-current 33 A1 configuration cases |
| Final `.venv\Scripts\python -m pytest tests/unit/test_config.py` | 0 | 37 passed in 0.47s, including empty query, empty fragment, empty port, and port-zero rejection |
| Final `.venv\Scripts\python -m pytest` | 0 | 184 passed in 11.26s: original 147 plus 37 final A1 configuration cases |
| A1 correction `.venv\Scripts\python -m pytest tests/unit/test_config.py` | 0 | 40 passed in 0.48s; PostgreSQL port `5432` is accepted and `bad`, `0`, and `65536` are rejected without DSN echo |
| A1 correction `.venv\Scripts\python -m pytest` | 0 | 187 passed in 11.25s: original 147 plus 40 corrected A1 configuration cases |
| A1 correction `.venv\Scripts\python -m dataguard.validation` | 0 | Stage 1 fixture/catalog baseline remains `status=ok`, 6/30/62, zero issues, and unchanged three fixture digests |
| `.venv\Scripts\python -m dataguard.validation` | 0 | Stage 1 fixture/catalog baseline remains `status=ok`, 6/30/62, zero issues, and unchanged three fixture digests |
| `.venv\Scripts\python -m compileall -q src tests` | 0 | All source and test modules compile |
| `git diff --check` | 0 | No tracked whitespace error |
| Changed-file UTF-8/BOM/LF/trailing-whitespace scan | 0 | All four A1 files pass |
| Markdown local-link scan | 0 | All 42 repository-local Markdown targets resolve |
| Secret heuristic and development-record database-URL scan | 0 | No token/private-key heuristic match and no database URL value in this record |

The first install timeout was a bounded slow-network event, not a product or
dependency incompatibility. A single longer retry completed; no third retry or
version substitution occurred. Editable-install `src/dataguard.egg-info`
metadata was removed from the worktree after verification.

## Data and security review

- Synthetic fixture/schema impact: none; no fixture, schema, digest, or Stage 1
  validation behavior changed.
- Evidence minimization impact: database DSNs use `SecretStr`, are excluded from
  repr/model dumps/JSON, and validation errors hide raw inputs. The config
  module defines no public response shape.
- Locality impact: URL parsing performs no DNS lookup and accepts only the three
  literal loopback forms over HTTP. Remote hostnames, alternate loopback text,
  credentials, query, fragment, and URL subpaths are rejected.
- Import-side-effect probe: the subprocess installs failing network, SQLite,
  directory-create, and file-touch hooks, supplies an invalid DataGuard
  environment value, imports `dataguard.config`, and observes no file creation.
- Secret/raw-content impact: tests use clearly synthetic credential sentinels
  only to prove those values do not appear in repr, serialization, or validation
  errors. No production credential, DSN, prompt, model body, or fixture literal
  was added to documentation or output.
- Dependency/supply-chain impact: direct dependencies are exact pins and were
  installed successfully. Transitive packages are not yet hash locked, so this
  candidate is not release/evidence ready.

## Failures and residual risks

1. The system pytest temporary root was inaccessible. The import-side-effect
   test now follows the accepted Stage 1 pattern and uses a temporary directory
   beneath ignored repository `.pytest_cache`; the complete suite passes.
2. Slow package download exceeded the first installation timeout. The bounded
   second attempt completed with exact pins. Fresh-install performance remains
   network-dependent until an approved hash-locked cache/workflow exists.
3. A1 validates configuration only. It does not prove an Ollama connection,
   PostgreSQL connection, ASGI application, API response, or evaluation run.
4. `database_dsn_value()` is intentionally an internal escape hatch for the
   later storage factory. Callers must not log, serialize, persist, or return
   its value; later storage/API tests must enforce that boundary.
5. Relative `artifacts/` paths are syntactically bounded without filesystem I/O.
   A later state/storage factory must resolve them against the trusted project
   root and reject symlink/reparse-point escape before writing.

## Handoff

- Files requiring independent test: `pyproject.toml`,
  `src/dataguard/config.py`, and `tests/unit/test_config.py`.
- Contract/gate cases affected: no public contract shape or evidence gate;
  S2-ENV locality, closure, secret-minimization, and exact dependency baseline
  only.
- Migrations/configuration required: none. Credentials and PostgreSQL DSNs must
  remain environment-only; A1 creates no database or local runtime state.
- Unresolved questions: none blocking A1. Hashed transitive lock generation and
  verification remains explicitly deferred.
- Next development context: stop at A1. A later separately assigned batch may
  implement versioned prompt/policy/detector resources or the fake-transport
  Ollama adapter; this record grants no authority to begin them.
- Git handoff: no `git add`, commit, or push was executed. Snapshot,
  independent testing, architecture review, and any release decision remain
  outside this development record.

No development record constitutes test acceptance, architecture acceptance,
release approval, commit, or push evidence.

## Stage 2 Batch A2a — Versioned security resources

### A2a metadata and boundary

- Baseline: clean `main` at
  `cc43cb2802aa442c8556d02399785e9a2b9c0a6a`, equal to `origin/main`
  before this batch.
- Requirement/work item: S2-RESOURCE, bounded Batch A2a only.
- Inputs rechecked: the complete
  [Stage 2 scope and acceptance](../architecture/STAGE2_SCOPE_AND_ACCEPTANCE.md),
  all machine contracts and the contract index, the committed A1 settings, and
  existing Stage 1 fixture/validation code.
- Scope: reviewed package resources, an explicit closed loader, exact-byte
  digests, package-data declaration, and resource-only unit tests.
- Explicit non-goals: Ollama adapter or request, prompt rendering/context
  assembly, detector execution, index/RAG/storage/API/evaluation/report/metrics,
  dependency lock, Compose, commit, and push.
- Public contract and architecture document changes: none.

This is development-side evidence only. It does not claim independent test or
architecture acceptance, and it does not claim that any Stage 2 runtime path is
implemented.

### A2a implementation summary

| File | Change | Boundary |
| --- | --- | --- |
| `pyproject.toml` | Declared `dataguard.resources` YAML files as package data | Makes reviewed resources available through installed-package resource APIs |
| `src/dataguard/resources/__init__.py` | Exported resource models, fixed constants, artifacts, loader, and safe error | Import performs no resource read |
| `src/dataguard/resources/loader.py` | Added duplicate-key-safe YAML parsing, frozen closed Pydantic resource models, fixed-constant/cross-resource validation, exact-byte SHA-256, and explicit `importlib.resources` loading | Does not render prompts, assemble messages, execute detection, or call a network |
| `src/dataguard/resources/system-prompt.v1.yaml` | Added the reviewed synthetic system prompt, one system Canary marker, and one opaque evidence ID | Marker literal remains only in this reviewed resource and runtime memory |
| `src/dataguard/resources/baseline-prompt.v1.yaml` | Declared the versioned weak single-user-payload template and placeholders | Declaration only; no interpolation implementation |
| `src/dataguard/resources/guarded-prompt.v1.yaml` | Declared separate system/document/query message slots and placeholders | Declaration only; documents remain designated untrusted JSON |
| `src/dataguard/resources/guard-policy.v1.yaml` | Locked models, roles, classifications, eight guarded stages, generation/retrieval settings, and fixed block reply | No enforcement implementation in A2a |
| `src/dataguard/resources/detector.v1.yaml` | Locked full-output scope, normalization order, zero-width set, detection types, mode actions, no raw-output persistence, and fixed block reply | Definition only; no detector execution in A2a |
| `tests/unit/test_resources.py` | Added 11 resource/package/digest/closure/format/minimization/import-side-effect tests | Dynamically reads the reviewed marker; never copies its literal into test source or failure text |

Resource digests over the exact committed-intent UTF-8/LF bytes are:

| Artifact | SHA-256 |
| --- | --- |
| `system_prompt` | `1010949fa467ba987b3b5822e121829af6566b16f940b33bc16c530c6748f48b` |
| `baseline_prompt_template` | `dfed1d7473f4d077ccbc83ef7ddd7cb74e69f1b987aaab8f190aacb581e0426e` |
| `guarded_prompt_template` | `4e61873d747da0c291e786a9a71b3bf0f1359cffa76cb2b7aa7c8234d7d7c566` |
| `guard_policy` | `8084394235aa27ebed03be15e31b5fe52a4ca8ca8e43148406834a912ef10f6f` |
| `detector` | `3137f394ad8d447db019d430427380f01abbdb7fb3e62854fd5d943be1915553` |

### A2a architecture conformance

- [x] Every resource declares version `1.0`, `synthetic=true`, and a closed
      resource type validated at explicit load time.
- [x] The system Canary marker appears exactly once in the repository's
      reviewed source set: the system prompt resource. Tests and this record
      never copy the literal.
- [x] The system Canary uses an opaque UUID evidence ID and does not collide
      with any document Canary value in `synthetic-v1`.
- [x] Baseline and guarded template resources declare only reviewed static text,
      placeholder names, and message slots. No untrusted-content formatting API
      exists in A2a.
- [x] Guard policy locks the approved models, settings, roles,
      classifications, eight guarded actions, and exact fixed block reply.
- [x] Detector definition locks untruncated full-output scanning, NFKC,
      casefold, the explicit five-code-point zero-width removal set, whitespace
      normalization, the three detection types, observe/block actions, and raw
      output non-persistence.
- [x] Loader input requires UTF-8 without BOM, LF without CR, duplicate-key-safe
      YAML, known filenames, and closed Pydantic models; failures expose only a
      fixed code and fixed safe message.
- [x] SHA-256 is computed over exact resource bytes, not a parsed or normalized
      representation.
- [x] Importing `dataguard.resources` performs no package-resource read or other
      I/O; loading is explicit.
- [ ] Prompt assembly, detector matching, system Canary injection into an Ollama
      request, and block enforcement remain intentionally unimplemented.
- [ ] Ollama adapter, hash-locked transitive dependency set, and all later
      Stage 2 components remain outside this batch.

### A2a development-side checks

| Command | Exit code | Result/evidence |
| --- | ---: | --- |
| First `.venv\Scripts\python -m pytest tests/unit/test_resources.py` | 1 | 10 passed and one test-setup failure: the import-side-effect probe intercepted Pydantic's unrelated plugin metadata read; no DataGuard resource read occurred |
| Corrected `.venv\Scripts\python -m pytest tests/unit/test_resources.py` | 0 | 11 passed in 1.56s after narrowing the probe to `importlib.resources.files` |
| `.venv\Scripts\python -m pip --disable-pip-version-check install -e . --no-deps --no-build-isolation` | 1 | The existing environment lacks the `setuptools` build backend; no dependency/version change was made |
| `.venv\Scripts\python -m pip --disable-pip-version-check install -e . --no-deps` | 124 | The one isolated retry reached the 120.4s hard limit while installing build dependencies; it was not retried and is recorded as an environment/network packaging limitation, not a product failure |
| `.venv\Scripts\python -m pytest` | 0 | 198 passed in 12.73s: the accepted 187-test A1 baseline plus 11 A2a resource tests |
| `.venv\Scripts\python -m dataguard.validation` | 0 | Stage 1 remains `status=ok`, 6 identities, 30 documents, 62 scenarios, and zero issues; fixture digests remain unchanged |
| `.venv\Scripts\python -m compileall -q src tests` | 0 | Source and test modules compile |
| Installed-environment `importlib.resources` read plus `load_security_resources()` | 0 | All five package resources are present, readable, closed-valid, and produce five digests |
| `git diff --check` | 0 | No tracked whitespace error |
| A2a UTF-8/BOM/LF/trailing-whitespace scan | 0 | All 10 changed or added text files are strict UTF-8, BOM-free, LF-only, and trailing-whitespace-free |
| Markdown local-link scan | 0 | All 45 repository-local Markdown targets resolve |
| Dynamic system Canary source-boundary scan | 0 | Exactly one source occurrence, in the reviewed system prompt resource; the marker value was not printed |
| Secret/credential heuristic scan | 0 | No private-key, access-key, assigned secret/token/password, or database-URL value match in A2a files |
| Contract/scope status boundary | 0 | No file under `docs/contracts/` and no `STAGE2_SCOPE_AND_ACCEPTANCE.md` change |

The editable-install attempts created only an empty ignored
`src/dataguard.egg-info` directory. It was removed after its exact empty state
was inspected; no generated build metadata is part of the handoff.

### A2a data and security review

- Synthetic data impact: no identity, corpus, scenario, or Stage 1 validation
  file changed; Stage 1 digests remain unchanged.
- Evidence minimization: no database, audit, report, error catalog, or public
  response is produced. Loader failures never expose resource bytes, parsed
  values, marker text, paths, or exception detail.
- Canary boundary: the marker literal exists only in the reviewed system prompt
  resource and transient runtime memory. Tests derive it dynamically and check
  that it does not escape to other text resources.
- Dependency/supply-chain impact: no dependency pin changed in A2a. Package-data
  installation could not be freshly rebuilt in the current bounded network
  window, while source-layout resource access and the complete installed target
  environment test suite pass. The deferred hash lock remains a release/evidence
  gate.

### A2a failures and residual risks

1. A fresh editable rebuild was not completed: no-build-isolation found no
   installed build backend, and the single isolated attempt timed out while
   acquiring build dependencies. A later packaging check must run from an
   approved cache/network environment and verify wheel/editable package-data
   inclusion without changing the exact build dependency.
2. A2a validates declarations and loading only. It does not show that any future
   Ollama request uses the resources, that guarded JSON/message isolation is
   assembled safely, or that detector normalization/blocking is correct.
3. The loader returns typed in-memory prompt/marker content to trusted internal
   callers. Later consumers must preserve the existing no-log, no-error,
   no-audit, no-report, and no-database literal boundary.
4. Transitive dependencies remain unhashed. Evidence/release readiness remains
   false until the separately authorized lock workflow succeeds.

### A2a handoff

- Files requiring independent test: `pyproject.toml`, all files under
  `src/dataguard/resources/`, and `tests/unit/test_resources.py`.
- Contract/gate cases affected: no public contract shape or evidence gate;
  A2a provides only versioned resource inputs and exact-byte artifact digests.
- Migrations/configuration required: none.
- Unresolved questions: none blocking A2a. Fresh package build/install evidence
  remains outstanding because of the bounded build-dependency timeout.
- Next development context: stop at A2a. The Ollama adapter and every other
  Stage 2 runtime component require a separately assigned batch.
- Git handoff: no `git add`, commit, or push was executed. Snapshot,
  independent testing, architecture review, and release decisions remain with
  the parent workflow.

No development record constitutes test acceptance, architecture acceptance,
release approval, commit, or push evidence.

### A2a architecture correction — baseline neutrality and evidence-ID isolation

- The baseline prompt template now contains only the neutral `SYSTEM TEXT`,
  `RETRIEVED DOCUMENT TEXT`, and `SYNTHETIC QUESTION` sections in one weak user
  payload. Its prior `untrusted` qualifier was removed because it could
  self-neutralize an indirect document injection and weaken the intended
  baseline comparison.
- The guarded prompt/template/policy/detector resources are unchanged.
- The baseline resource SHA-256 changed from
  `cd272717132aabdc6858d5f8f773af8a1e9f8788025b01a49edfaf8f6c8a5e00` to
  `dfed1d7473f4d077ccbc83ef7ddd7cb74e69f1b987aaab8f190aacb581e0426e`.
- The resource test now rejects a fixed English/Chinese set of document-warning
  and self-neutralizing terms in the baseline template.
- The system Canary opaque evidence ID is now dynamically checked against every
  corpus `canary_id` and `fragment_id`, in addition to the existing marker-value
  separation check. Neither its marker literal nor its ID is copied into the
  test or this record.
- No editable-install retry was performed. No Ollama adapter or other A2b work
  was started.

Correction verification:

| Command | Exit code | Result/evidence |
| --- | ---: | --- |
| `.venv\Scripts\python -m pytest tests/unit/test_resources.py` | 0 | 11 passed in 1.53s, including baseline neutral-term and evidence-ID collision assertions |
| `.venv\Scripts\python -m pytest` | 0 | 198 passed in 12.83s |
| `.venv\Scripts\python -m dataguard.validation` | 0 | Stage 1 remains `status=ok`, 6/30/62, zero issues, and unchanged fixture digests |

No development record constitutes test acceptance, architecture acceptance,
release approval, commit, or push evidence.
